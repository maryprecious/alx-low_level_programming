#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point for single digit.
 *
 * Return: 0 Always (success)
 */
int main(void)
{
int num;
while (num < 10)
{
printf("%d", num);
num += 1;
}
printf("\n");
return (0);
}
